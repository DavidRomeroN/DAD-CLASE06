server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DavidRomeroN/DAD-CLASE06.git # ruta del repositorio git
          searchPaths: config-data
          default-label: master
  security:
    user:
      name: root
      password: 123456